#include<bits/stdc++.h>
#define int long long
using namespace std;

signed main(){
    int n;
    cin >> n;
    vector<pair<int,int>> a(n);
    for(auto& [x,y]: a)cin >> x >> y;
    multiset<pair<int,int>> tg,bleh;
    for(auto& [x,y]: a){
        if(y == 1)
    }

    return 0;
}