#include<bits/stdc++.h>
#define int long long
using namespace std;

int main(){
    int t;
    cin >> t;
    while(t--){
        int p,f,a,b,s,w;
        cin >> p >> f >> a >> b >> s >> w;
        vector<int> A(a+1),B(a+1);
        for(int i = 0; i < s; ++i){
            
        }

    }
}