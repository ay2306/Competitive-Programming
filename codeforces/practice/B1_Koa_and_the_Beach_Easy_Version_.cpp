#include<bits/stdc++.h>
#define int long long
using namespace std;

signed main(){
    int t;
    scanf("%lld",&t);
    while(t--){
        int n,k,l;
        scanf("%lld%lld%lld",&n,&k,&l);
        vector<int> d(n);
        for(int &i: d)scanf("%lld",&i);
        
    }
}