#include<bits/stdc++.h>
using namespace std;

int main(){
    int w,r,a,b,c;
    while(scanf("%d",&w) != EOF){
        vector<pair<int,int>> work;
        for(int i = 0; i < w; ++i){
            scanf("%d%d",&a,&b);
            work.emplace_back(a,b);
        }
    }
}